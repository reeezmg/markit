# Add these values to the Storetools deployment environment; do not commit real secrets.
# The same SMTP mailbox is used for OTP and marketing. Configure sender reputation/DNS separately.
SMTP_HOST=smtpout.secureserver.net
SMTP_PORT=465
SMTP_USER=no-reply@example.com
SMTP_PASS=
CRON_SECRET=
MARKETING_LINK_SECRET=
MARKETING_PUBLIC_BASE_URL=https://admin.example.com
