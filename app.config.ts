export default defineAppConfig({
    ui: {
        menu:{},
        primary: 'orange',
        gray: 'cool',
        tooltip: {
            default: {
                openDelay: 500,
            },
        },
    },
});
