export default defineAppConfig({
    ui: {
        primary: 'orange',
        gray: 'cool',
        tooltip: {
            default: {
                openDelay: 500,
            },
        },
    },
});
